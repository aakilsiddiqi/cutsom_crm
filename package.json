{
  "private": true,
  "scripts": {
    "start": "cd Frontend && npx expo start",
    "android": "cd Frontend && npx expo start --android",
    "ios": "cd Frontend && npx expo start --ios",
    "web": "cd Frontend && npx expo start --web"
  },
  "dependencies": {
    "expo": "^57.0.8"
  },
  "devDependencies": {
    "@types/react": "~19.2.4",
    "typescript": "~6.0.3"
  }
}
